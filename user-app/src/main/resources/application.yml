spring:
  application:
    name: scalable-task-manager-user-app
  datasource: # To automatically set datasource info for Spring
    url: jdbc:postgresql://localhost:5432/stm_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: admin
  jpa:
    open-in-view: false
    generate-ddl: false
    hibernate:
      ddl-auto: none # Hibernate doesn't influence tables
      naming: # To prevent Hibernate from naming columns
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    url: /api/swagger.yml